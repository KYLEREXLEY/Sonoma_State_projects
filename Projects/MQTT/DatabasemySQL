-- Table to store potentiometer values
CREATE TABLE IF NOT EXISTS sensor_value (
    id INT AUTO_INCREMENT PRIMARY KEY,
    value DOUBLE NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
